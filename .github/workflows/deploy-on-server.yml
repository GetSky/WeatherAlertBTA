name: Deploy on server

on:
  workflow_dispatch:

jobs:
  deployment:
    runs-on: ubuntu-latest
    environment: develop
    steps:
      - name: deploy
        run: |
          echo "Image: ${{ vars.IMAGE_NAME }}:${{ vars.IMAGE_TAG }}"